# 变更记录

> 记录代码发生了什么变化，防止重复修改和重复提交。当前活动记录只保留现行主线相关变更。

---

## 模板

### [时间]
- 任务名称：
- 修改文件：
- 具体改动：
- 修改原因：
- 是否影响训练/推理：
- 是否已提交 Git：
- 对应 commit：

---

## 实际记录

### [2026-04-21 22:40 UTC]
- 任务名称：Phase 2.5 T2-only diagnosis、efficiency defense 与 representative cases
- 修改文件：
  - `claim_driven实验路径.md`
  - `docs/Phase2_5_T2DiagnosisDefense.md`
  - `docs/Phase2_3_FixedBudgetFull14.md`
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `figures/phase2_5_t2_performance_cost.pdf`
  - `figures/phase2_5_t2_performance_cost.png`
  - `figures/phase2_5_t2_representative_cases.pdf`
  - `figures/phase2_5_t2_representative_cases.png`
  - `findings.md`
  - `memory/STAGE_MEMORY.md`
  - `refine-logs/EXPERIMENT_PLAN.md`
  - `refine-logs/EXPERIMENT_TRACKER.md`
  - `scripts/phase2_5_t2_diagnosis.py`
  - `scripts/phase2_5_t2_qualitative.py`
- 具体改动：
  - 新增 Phase 2.5 诊断脚本，基于 `3`-seed `T0/T2` case-level 结果生成：
    - size strata 表
    - boundary strata 表
    - efficiency defense 表
    - representative case 候选
    - performance-cost 图
  - 新增 representative case 可视化脚本，复用 seed `42` checkpoint 生成 `T0 vs T2` 定性对比图
  - 新增 `docs/Phase2_5_T2DiagnosisDefense.md`
  - 将活动规划、tracker、findings 和记忆更新到“Phase 2.5 已完成，下一步进入 paper-ready claim freeze”
- 修改原因：
  - 把主线从“`T2` 平均更好”推进到“`T2` 为什么更好、在哪些结构上更好、代价是否合理”
- 是否影响训练/推理：
  - 否，没有新增训练
  - 是，新增了复用现有 checkpoint 的分析和定性可视化脚本
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 21:46 UTC]
- 任务名称：Phase 2.4 T2 confirmation 与 T2-only 主线收束
- 修改文件：
  - `claim_driven实验路径.md`
  - `docs/Phase2_4_T2Confirmation.md`
  - `docs/Phase2_B系最小增强Run.md`
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `findings.md`
  - `memory/STAGE_MEMORY.md`
  - `refine-logs/EXPERIMENT_PLAN.md`
  - `refine-logs/EXPERIMENT_TRACKER.md`
- 具体改动：
  - 读取并固化 Phase 2.4 的 `3`-seed `T0 vs T2` 汇总结果
  - 新增 `docs/Phase2_4_T2Confirmation.md`，集中记录 mean +- std、pooled win rate、go/no-go 与主方法决策
  - 将 claim-driven 主规划和 experiment plan 正式收缩到 `T2 = HSF-only`
  - 将 `T3` 标记为封存分支，不再作为活动目标
  - 将旧 `Phase2_B系最小增强Run.md` 改写为历史封存说明，防止后续误回到 `B3` 叙事
  - 新增 `findings.md`，以 `result-to-claim` 口径记录当前本地 verdict
- 修改原因：
  - Phase 2.4 已经回答“`T2` 能不能成为唯一主方法”这个问题，后续必须避免继续围绕 `T3` 摆动
- 是否影响训练/推理：
  - 否，本次为结果分析、规划收束和记忆压缩
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 21:29 UTC]
- 任务名称：Phase 2.3 full-14 bridge validation 与 win-rate 分析
- 修改文件：
  - `docs/Phase2_3_FixedBudgetFull14.md`
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `memory/STAGE_MEMORY.md`
  - `refine-logs/EXPERIMENT_TRACKER.md`
- 具体改动：
  - 使用现有 fixed-budget bridge-only checkpoint 直接完成 `T0/T1/T2/T3` 的 full `14` test item 评测
  - 补充 full-14 主指标、delta vs `T0`、win rate vs `T0` 和 go/no-go 结论
  - 将 Phase 2.3 的核心结论单独整理成阶段文档
- 修改原因：
  - 在不改 recipe、不扩实验面的前提下，验证 Phase 2.2 的正向 smoke 信号是否能在更完整评测面上站住
- 是否影响训练/推理：
  - 否，本次主要是固定 checkpoint 的完整评测与分析
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 21:10 UTC]
- 任务名称：Phase 2.2 bridge-only 训练入口与 learnability smoke
- 修改文件：
  - `configs/phase2_2_trainable_bridge.yaml`
  - `configs/phase2_2_bridge_eval.yaml`
  - `docs/Phase2_2_TrainableBridge.md`
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `memory/STAGE_MEMORY.md`
  - `refine-logs/EXPERIMENT_TRACKER.md`
  - `scripts/phase2_2_bridge_audit.py`
  - `scripts/phase2_b_minimal_eval.py`
  - `external/IMIS-Bench/model.py`
  - `external/IMIS-Bench/train.py`
  - `external/IMIS-Bench/segment_anything/build_sam.py`
  - `external/IMIS-Bench/segment_anything/modeling/image_encoder.py`
- 具体改动：
  - 为 `IMISNet` 增加 `set_trainable_scope()`、全冻结与 trainable summary 能力
  - 为 `train.py` 增加：
    - `trainable_scope`
    - `local_adapter_blocks / hsf_blocks`
    - `max_steps_per_epoch`
    - `disable_text_prompt`
    - `skip_interaction_training`
  - 为 evaluator 增加 method-specific checkpoint 和训练态 wrapper checkpoint 整模型回填能力
  - 新增 Phase 2.2 配置与 bridge audit 脚本
  - 新增 `docs/Phase2_2_TrainableBridge.md`
  - 修复 Local Adapter 的 dead-zero init：
    - 从 depthwise=`0`, pointwise=`0`
    - 改为 depthwise identity-like, pointwise=`0`
- 修改原因：
  - 将当前问题收缩到“bridge-only 能否学习”，并排除 text tokenizer、interaction detach 和 adapter 死梯度等无关阻塞
- 是否影响训练/推理：
  - 是，当前可以正式进行 freeze-A2 的 bridge-only 最小训练
  - 否，B 线未训练状态仍保持零扰动输出
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 20:41 UTC]
- 任务名称：B 线最小实现、统一 evaluator 与 B0 冻结确认
- 修改文件：
  - `configs/phase2_b_methods.yaml`
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `memory/STAGE_MEMORY.md`
  - `refine-logs/EXPERIMENT_TRACKER.md`
  - `scripts/imisbench_smoke.py`
  - `scripts/phase2_b_minimal_eval.py`
  - `scripts/phase2_minimal_eval.py`
  - `external/IMIS-Bench/model.py`
  - `external/IMIS-Bench/test.py`
  - `external/IMIS-Bench/train.py`
  - `external/IMIS-Bench/segment_anything/build_sam.py`
  - `external/IMIS-Bench/segment_anything/modeling/image_encoder.py`
  - `scripts/phase1_baseline_eval.py`
- 具体改动：
  - 为 `ViT` 后段加入 `LateStageLocalAdapter`、多层 feature taps 和 optional `hierarchical_embeddings` 输出
  - 为 `IMISNet` 增加 `HierarchicalSemanticFusion`、dict/tensor 双路径兼容、`detach_image_embedding()` 与 decoder embedding resolve 逻辑
  - 将 `build_sam.py` 改为可注入 `local_adapter_blocks / hsf_blocks`，并使用 `strict=False` 加载旧 checkpoint
  - 补齐 `train.py`、`test.py`、`phase1_baseline_eval.py` 对新 image embedding 结构的兼容
  - 新增 `configs/phase2_b_methods.yaml` 与 `scripts/phase2_b_minimal_eval.py`，统一输出 `Dice@3/5/8`、`NoC@90`、latency、memory 和 B0 freeze check
  - 更新 `scripts/imisbench_smoke.py` 以直接按 `B0-B3` 配置做 smoke
  - 删除旧失败主线遗留脚本 `scripts/phase2_minimal_eval.py`
- 修改原因：
  - 将当前活动主线真正落到代码和评测入口，先完成“零扰动接通 + B0 不漂移”的最小工程闭环
- 是否影响训练/推理：
  - 是，IMIS-Bench 的 image encoder 现在可选输出多层特征并支持后段 adapter / HSF
  - 否，`B0` 默认路径保持与 A2 等价
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 12:05 UTC]
- 任务名称：切换到 B 线并重写实验规划
- 修改文件：
  - `AGENTS.md`
  - `claim_driven实验路径.md`
  - `docs/Phase1_A2基线合法化.md`
  - `docs/Phase2_B系最小增强Run.md`
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `docs/提交规范.md`
  - `memory/STAGE_MEMORY.md`
  - `refine-logs/EXPERIMENT_PLAN.md`
  - `refine-logs/EXPERIMENT_TRACKER.md`
- 具体改动：
  - 使用 `experiment-plan` 思路将当前论文主线收束为：
    - Late-Stage Local Adapter
    - Hierarchical Semantic Fusion
    - optional Light Prompt-Aware Weighting
  - 重写 claim-driven 主规划，冻结新的最小实验矩阵 `B0 / B1 / B2 / B3`，并将 `B4` 降级为 optional supporting variant
  - 新增 B 线阶段文档，以及 `refine-logs/EXPERIMENT_PLAN.md` 与 `EXPERIMENT_TRACKER.md`
  - 从活动文档中移除上一轮失败主线的记录，只保留 Phase 0 / 1 地基与当前有效主线
- 修改原因：
  - 终止旧失败主线，避免继续在不成立的 claim 上扩表，并把后续实现和训练流程规范化
- 是否影响训练/推理：
  - 否，本次以规划、记录和规范重写为主
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 09:39 UTC]
- 任务名称：进入 Phase 1 并完成 A0/A1/A2 baseline legalization
- 修改文件：
  - `configs/phase1_baselines.yaml`
  - `docs/Phase1_A2基线合法化.md`
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `memory/STAGE_MEMORY.md`
  - `scripts/phase1_baseline_eval.py`
- 具体改动：
  - 新增 Phase 1 基线定义配置与统一 legalization 评测脚本
  - 在 local BTCV sample 上运行 A0/A1/A2 sweep，输出 JSON/CSV/Markdown 结果表
  - 冻结当前唯一强基线：`B0 = A2 = points K=8`
- 修改原因：
  - 将 claim-driven 的下一步从口头规划推进到可执行、可复测、可续跑的基线合法化阶段
- 是否影响训练/推理：
  - 是，后续所有 B 系实验都会统一继承 A2 基线入口
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 09:26 UTC]
- 任务名称：补齐 IMIS-Bench 单卡日志链路并完成官方 points 评测
- 修改文件：
  - `docs/实验启动记录.md`
  - `docs/变更记录.md`
  - `memory/STAGE_MEMORY.md`
  - `external/IMIS-Bench/train.py`
  - `external/IMIS-Bench/test.py`
- 具体改动：
  - 为 `train.py` 和 `test.py` 增加单卡模式日志初始化
  - 使用官方 `test.py` 完成 `points` 模式 sample 评测
  - 将评测命令、日志路径和关键结果回写到项目文档
- 修改原因：
  - 满足 Phase 0 对“官方入口可跑 + 有固定日志”的要求
- 是否影响训练/推理：
  - 是，单卡训练/测试现在会稳定生成日志文件
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无

### [2026-04-21 09:23 UTC]
- 任务名称：Phase 0 环境搭建、ARIS 接入与 IMIS-Bench smoke 复现
- 修改文件：
  - `.gitignore`
  - `AGENTS.md`
  - `configs/paths.example.yaml`
  - `docs/ARIS_Codex_接入说明.md`
  - `docs/实验启动记录.md`
  - `memory/STAGE_MEMORY.md`
  - `scripts/github_snapshot_sync.py`
  - `scripts/imisbench_smoke.py`
  - `scripts/setup_aris_mcp.sh`
  - `external/IMIS-Bench/model.py`
  - `external/IMIS-Bench/test.py`
- 具体改动：
  - 修复 GitHub 快照同步脚本的 raw URL 编码问题，补齐 `BTCV` sample 数据
  - 创建 IMIS-Bench conda 环境并安装 5090 可运行的依赖组合
  - 建立本地路径模板、项目级 `AGENTS.md` 与 ARIS/Codex 接入说明
  - 创建 `ckpt/IMISNet-B.pth` 软链接与 `scripts/imisbench_smoke.py`
  - 将 IMIS-Bench 的 text tokenizer 改为按需加载，并让 `test.py` 只在 text 模式生成 text prompt
- 修改原因：
  - 打通 Phase 0 所需的最小可复现链路，并避免默认 `points` 流程被外部网络阻断
- 是否影响训练/推理：
  - 是，默认 `points` / `bboxes` 测试路径现在可在离线条件下运行
- 是否已提交 Git：
  - 否
- 对应 commit：
  - 暂无
